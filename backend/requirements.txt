# Web framework
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.9
aiofiles>=23.2.1
pydantic>=2.6.0
pydantic-settings>=2.2.0

# PII detection (Microsoft Presidio - free, MIT-licensed, runs fully local)
presidio-analyzer>=2.2.354
presidio-anonymizer>=2.2.354
# Pinned to the range models/en_core_web_lg_pruned declares in its meta.json
# ("spacy_version": ">=3.8.0,<3.9.0"). Unpinned, this eventually resolves to
# spacy 4.x, which refuses to load that model and breaks the deploy.
spacy>=3.8.0,<3.9.0
# The deployed image uses the committed models/en_core_web_lg_pruned. For a
# local install you can instead use the full model:
#   python -m spacy download en_core_web_lg

# Document processing
PyMuPDF>=1.23.0
python-docx>=1.1.0
Pillow>=10.2.0
pytesseract>=0.3.10
# Also requires the Tesseract OCR system binary (not a pip package):
#   macOS:  brew install tesseract
#   Ubuntu: apt-get install tesseract-ocr

# Testing
pytest>=8.0.0
pytest-asyncio>=0.23.0
httpx>=0.27.0
